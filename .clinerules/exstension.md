Create a Visual Studio Code extension that is fully compatible with VS Code Web (running in the browser). The extension should:  
1. Use only web-compatible APIs (no Node.js or native modules).  
2. Include a `browser` entry point in `package.json`.  
3. Avoid dependencies that require file system or OS-level access.  
4. Provide a minimal working example demonstrating its functionality in VS Code Web.  
5. Include clear instructions for testing the extension in VS Code Web using `vscode.dev` or GitHub Codespaces.